<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8">
  <title>Modern Híroldal</title>
  <style>
    /* CSS stílusok */
    :root {
      --bg: #f5f6f8;
      --text: #1f2933;
      --muted: #6b7280;
      --accent: #2563eb;
      --card: #ffffff;
      --border: #e5e7eb;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.6;
    }

    header {
      background: var(--card);
      border-bottom: 1px solid var(--border);
      padding: 1rem 2rem;
      font-size: 1.5rem;
      font-weight: 700;
    }

    .container {
      display: grid;
      grid-template-columns: 1fr 300px;
      gap: 2rem;
      padding: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }

    nav {
      background: var(--card);
      border: 1px solid var(--border);
      padding: 1rem;
      margin-bottom: 2rem;
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 1.5rem;
      flex-wrap: wrap;
    }

    nav li {
      font-weight: 600;
      color: var(--muted);
      cursor: pointer;
    }

    nav li:hover {
      color: var(--accent);
    }

    .articles {
      display: grid;
      gap: 1.5rem;
    }

    article {
      background: var(--card);
      border: 1px solid var(--border);
      padding: 1.5rem;
    }

    article h2 {
      font-size: 1.25rem;
      margin-bottom: 0.5rem;
    }

    article .meta {
      font-size: 0.875rem;
      color: var(--muted);
      margin-bottom: 0.75rem;
    }

    article p {
      color: #374151;
    }

    aside {
      background: var(--card);
      border: 1px solid var(--border);
      padding: 1.5rem;
      height: fit-content;
    }

    aside h3 {
      margin-bottom: 1rem;
      font-size: 1.1rem;
    }

    aside ul {
      list-style: none;
    }

    aside li {
      padding: 0.5rem 0;
      border-bottom: 1px solid var(--border);
      font-size: 0.95rem;
    }

    aside li:last-child {
      border-bottom: none;
    }

    footer {
      text-align: center;
      padding: 2rem;
      color: var(--muted);
      font-size: 0.875rem;
    }

    @media (max-width: 900px) {
      .container {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>

<header>
  Modern Hírek
</header>

<nav>
  <ul>
    <li>Széchenyi</li>
    <li>→ Világ</li>
    <li>→ Vélemények</li>
    <li>→ Biznisz</li>
    <li>→ Szórakozás</li>
  </ul>
</nav>

<div class="container">
  <main class="articles" id="articles-container">
    <!-- Cikkek itt jelennek meg -->
  </main>

  <aside>
    <h3>Legolvasottabb</h3>
    <ul id="most-read-list">
      <!-- Legolvasottabb cikkek itt -->
    </ul>
  </aside>
</div>

<footer>
  © 2026 Modern Hírek
</footer>

<script>
  // Cikkek betöltése
  async function loadArticles() {
    const response = await fetch('/articles');
    const articles = await response.json();

    const articlesContainer = document.getElementById('articles-container');
    articlesContainer.innerHTML = ''; // Kiüríti az eddigi cikkeket

    articles.forEach(article => {
      const articleElement = document.createElement('article');
      articleElement.innerHTML = `
        <h2>${article.title}</h2>
        <div class="meta">${article.category_id} • ${article.date}</div>
        <p>${article.body.slice(0, 150)}...</p>
      `;
      articlesContainer.appendChild(articleElement);
    });
  }

  // Az oldal betöltésekor hívjuk meg a cikkeket
  window.onload = loadArticles;
</script>

</body>
</html>
